---
# Root app-of-apps. Points at one of the overlays under gitops/overlays/.
# bootstrap.sh substitutes REPO_URL, TARGET_REVISION, and OVERLAY at apply time;
# you can also commit this file with values filled in if you don't want the wrapper.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rhoai-2254-root
  namespace: openshift-gitops
  labels:
    # Selected by the RHPDS gitops-bootstrap health check
    # (label_selectors: app.kubernetes.io/instance=bootstrap-infra).
    # The overlays stamp this same label on every child Application.
    app.kubernetes.io/instance: bootstrap-infra
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: ${REPO_URL}
    targetRevision: ${TARGET_REVISION}
    path: rhoai-2254-install/gitops/overlays/${OVERLAY}
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-gitops
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 10
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
